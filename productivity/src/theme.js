import { createMuiTheme } from '@material-ui/core/styles';


const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#2D3436',
      contrastText: '#fff',
    },
    secondary: {
      main: '#b2dfdb',
    },
  },
});

export default theme;